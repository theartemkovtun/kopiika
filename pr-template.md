<!--
PR description template. Fill in each section, then delete these comments.
Keep it short: a reviewer should understand the change without opening the diff.
Write every section as bullet points (`- ...`), not prose paragraphs — one
point per bullet.
-->

## Why

<!--
The problem or goal behind this change: what is broken, missing, or being
improved, and why it matters now. Link the issue if there is one.
Describe the motivation, not the implementation.
-->

## What

<!--
What the change actually does, at the level a reviewer needs to follow the
diff: the approach taken, the main files or layers touched (`api/`, `app/`),
and anything notable such as new migrations, env vars, endpoints, or
regenerated Swagger docs. Mention how it was verified (checks run, manual
testing).
-->

## Risks

<!--
What could go wrong once this merges, and how to back it out: data or schema
changes, breaking API or wire-format changes, behaviour changes users will
notice, config that must be set before deploy, and areas that were not tested.
If there is genuinely no meaningful risk, say so and why.
-->
